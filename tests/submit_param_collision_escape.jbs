param p {
  nodes = (1,2)
  a = (1,2)
  b = ("x","y")
  b * (a + nodes)
}

submit run with p {
  account = "atmlaml"
  queue = "batch"
  nodes = "${nodes}"
  preprocess = {
    echo ${a} ${b} ${nodes}
    echo $nodes
  }
  args_exec = "-lc 'echo ${nodes} ${a} ${b}'"
}

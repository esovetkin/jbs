param p {
  a = (1,2)
  b = ("x","y","z")
  c = ("u","v","w")
  a * (b + c)
}

do s0 with a from p {
  echo ${a}
}

do s1 after s0 with b from p {
  echo ${a} ${b}
}

do s2 after s1 with c from p {
  echo ${a} ${b} ${c}
}

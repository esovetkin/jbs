ERROR E023 /home/jackson/jülich/jbs_review2/tests/inherit_keyword_rejected.jbs:11:9
JBS does not use `inherit`; `after` already carries predecessor-visible names
    11 |         inherit prep
       |         ^
    12 | {
Hint: remove `inherit` and use `after <step>` for predecessor dependencies

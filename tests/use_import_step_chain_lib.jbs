let submit_defaults
{
        queue = "batch"
}

param p
{
        a = (1, 2)
        a
}

do prep
        with p
{
        echo "${a}" > prep.out
}

submit run
        after prep
        use submit_defaults
        with p
{
        args_exec = "-lc echo ${a}"
}

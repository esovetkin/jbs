param p
{
        x = (1, 2)
        y = ("left", "right")
        x * y
}

do run_in
        with (x, y) in p
{
        echo ${x} ${y}
}

do run_slice
        with p[x, y]
{
        echo ${x} ${y}
}

param matrix {
  case = ("ddp","fsdp")
  nnodes = (1,2)
  case + nnodes
}

do setup with case from matrix {
  echo setup ${case}
}

submit run after setup with matrix {
  preprocess = {
    export X=1
  }
  args_exec = "python main.py --case ${case} --nnodes ${nnodes}"
}
